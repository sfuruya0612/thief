server {
    listen 8088;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA フォールバック: API/WebSocket は backend (THIEF_LISTEN_ADDR) へ直接アクセスするため
    # リバースプロキシは設けない (CORS 反射と THIEF_WEB_ORIGINS で対応する)。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
